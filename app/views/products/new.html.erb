<% content_for :title, "ODDS SHOP > เพิ่มสินค้า" %>

<div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
  <h1 class="font-bold text-3xl sm:text-4xl text-gray-800 mb-6 text-center">
    🛒 เพิ่มสินค้าใหม่
  </h1>

  <div class="bg-gray-100 p-6 rounded-lg">
    <%= render "form", product: @product %>
  </div>

  <div class="mt-6 flex flex-col sm:flex-row justify-center gap-4">
    <%= link_to "กลับไปหน้าสินค้า", products_path, class: "w-full sm:w-auto text-center rounded-md px-5 py-3 bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold transition-all duration-300 shadow-md" %>
  </div>
</div>
